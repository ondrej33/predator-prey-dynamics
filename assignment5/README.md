# Assignment 5

Original interactive demo <a href="http://jsfiddle.net/roman01la/LZKVM/9/embedded/result/">DEMO</a>

## Installation and run
* install node

### Build

* install npm and bower
* run `npm install` (install packages from package.js)
* run `bower install` (install packages from bower.js)

### Running
* for starting server run `node index.js`. App will run on `localhost:5000`
* if you want just check changes in code, u don't need to 
